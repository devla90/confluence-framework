@AGENTS.md

<!-- Compatibility shim. Claude Code does not read AGENTS.md natively yet, so this file
     imports it on the line above. It exists to bring Claude Code level with the other
     assistants, which read AGENTS.md on their own — not to give it special standing.
     Everything about the framework lives in AGENTS.md; everything Claude-specific lives
     in adapters/claude-code/. -->

Claude Code specifics — the `/doc-confluence` skill, the `confluence-doc` agent, how to
install them, and how the procedure's tool-neutral wording maps to Claude Code's tools —
are in [`adapters/claude-code/README.md`](adapters/claude-code/README.md).
